# Stack-level variables for docker/compose.yml (per environment).
# Copy to docker/.env (production) or .env.staging, then fill in.
DOMAIN=orders.example.com
POSTGRES_USER=app
POSTGRES_PASSWORD=
POSTGRES_DB=app
